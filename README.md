# V-Concept Website

Website Node.js thuần được dựng theo thiết kế Figma `V-Concept`.

## Chạy local

```bash
node server.js
```

Mặc định server chạy ở `http://localhost:3001`. Nếu cổng này bận, server tự chuyển sang cổng kế tiếp.
